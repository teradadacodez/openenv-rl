[project]
name = "openenv-agent"
version = "0.1.0"
description = "OpenEnv agent submission"
requires-python = ">=3.10"
dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
entrypoint = "inference.py"